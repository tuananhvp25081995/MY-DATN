extends ../layouts/common

block content
  table.table
    tr
      th Image
      th Name
      th Amount
      th price
    //- each user in users
    //-   tr
    //-     td
    //-       img(src=user.avatar, alt="",width="32")
    //-     td=user.name
    //-     td=user.phone
    //-     td
    //-       a.btn-btn-light(href='/users/'+ user.id) View